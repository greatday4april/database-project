# CS631 Database Design App

## run the app (with Django framework)

1. install python
2. `pip install pipenv`
3. cd into the project folder and `pipenv shell`
4. `pipenv install`
5. run `python3 manage.py runserver`, then visit `http://127.0.0.1:8000`

## Sample REST APIs and SQLs for each functionality
